// This file contains all constants values that the app uses. Avoids hardcoding strings and numbers in the codebase.

// Useful to avoid hardcoding strings for revalidation keys in the cache
export const TAGS_KEYS = {
	USER_PROFILE: 'user-profile',
};
